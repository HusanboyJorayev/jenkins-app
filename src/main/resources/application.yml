spring:
  application:
    name: jenkins

  cache:
    type: redis

#  data:
#    redis:
#      host: redis          # Docker-compose bo‘yicha hostname
#      port: 6379
#      timeout: 6000ms
#      lettuce:
#        pool:
#          max-active: 10
#          max-idle: 5
#          min-idle: 1
#          max-wait: 3000ms

server:
  port: 8765
  shutdown: graceful
  servlet:
    context-path: "/jenkins"

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs           # backend API docs path
  swagger-ui:
    enabled: true
    path: /swagger-ui            # Swagger UI path
    url: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
